# PlayStatus Homebrew Tap

This is the official Homebrew Cask tap for PlayStatus.

Install the current release with:

```sh
brew install --cask nbolar/playstatus/playstatus
```

PlayStatus updates itself through Sparkle. To have Homebrew explicitly replace
an auto-updating cask, run:

```sh
brew upgrade --cask --greedy playstatus
```
